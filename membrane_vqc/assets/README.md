# Assets

This folder is reserved for future plugin icons, screenshots, or small PyMOL demo assets.

The MVP intentionally ships without binary assets so the command API remains the primary,
testable interface.
