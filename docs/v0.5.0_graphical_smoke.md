# v0.5.0 exact release-artifact graphical smoke

Overall result: **PENDING OWNER ACCEPTANCE**.

This is the single bounded manual checkpoint for the exact final CI-built
`MembraneVisualQC-0.5.0.zip`. Automated or earlier development-artifact observations do not replace
this release-artifact check.

## Artifact and environment evidence

- final release PR head: **PENDING**
- CI workflow / artifact ID / outer artifact digest: **PENDING**
- inner Plugin ZIP size / SHA-256: **PENDING**
- sidecar verification: **PENDING**
- archive safety and provider-payload exclusion: **PENDING**
- OS: Windows 10 build 26200
- PyMOL: Incentive PyMOL 3.1.8
- bundled CPython: 3.10.20

Before the owner checkpoint, install this exact byte-verified ZIP through PyMOL Plugin Manager and
completely restart PyMOL. Disable any checkout-loaded development copy so the displayed code is
unambiguously the installed artifact.

## One owner checklist

1. Confirm the installed plugin displays version `0.5.0`, the dialog opens, and Unicode text renders
   correctly.
2. Run the Legacy global-z workflow and verify the slab, hydropathy, ligand shell, JSON/CSV export,
   and expected conservative review presentation.
3. Run the planar-orientation workflow and verify the arbitrary-plane slab and export.
4. Run an explicit local PDBTM pair workflow and verify schema 1.3 output.
5. Inspect and explicitly use the validated cached PDBTM pair; verify that **Use cached pair** does
   not perform a fetch and that QC/Show Slab/export produce schema 1.4 evidence.
6. Verify **Fetch / Refresh** is visibly distinct from cache use and is the only action that
   authorizes network access.
7. Run the explicit local PDBTM–OPM comparison and verify schema 1.5 output.
8. Verify both sources' four comparison-owned boundary planes are visible and distinguishable.
9. Verify comparison wording presents geometric review bands only and contains no automatic source
   selection, provider ranking, consensus, or biological correctness verdict.
10. Verify no automatic fit/alignment occurs and the selected input object's coordinates are
    unchanged before and after every tested workflow.
11. Verify Clear removes only plugin-owned output and preserves the input object.
12. Start and cancel a bounded operation, then close/reopen the dialog; verify stale results are not
    applied and PyMOL remains responsive without a QThread warning or crash.
13. Verify invalid/local mismatch cases fail clearly without leaving stale slab, comparison, or
    export state.
14. Confirm OPM remains local/offline-only and no OPM retrieval action is offered.

Reply exactly `PASS` if every item passes, or provide the failed checklist numbers. Record the
owner response, observation date, and any sanitized failure evidence here.

## Bounded live PDBTM release acceptance

One `1pcr` fetch is permitted outside the ordinary test gate:

- exactly two GETs (`pdbtm_json`, then `transformed_pdb`);
- zero retries and zero redirects;
- no proxy discovery, PAC, CONNECT, or proxy credentials;
- ordinary TLS verification;
- payload byte sizes and SHA-256 must match accepted evidence;
- no live OPM retrieval.

Request count, payload identities, cache commit/use result, and forced-offline reuse result:
**PENDING**.

## Final accepted evidence

Fill only after the exact artifact passes:

- owner response and date: **PENDING**
- plugin version/dialog/Unicode: **PENDING**
- Legacy/Planar/local-PDBTM/cached-PDBTM/comparison workflows: **PENDING**
- schema 1.3/1.4/1.5 exports: **PENDING**
- slab and comparison boundaries: **PENDING**
- hydropathy and ligand shell: **PENDING**
- clear isolation and cancellation/close lifecycle: **PENDING**
- coordinate preservation: **PENDING**
- absence of fitting, automatic selection, ranking, consensus, and biological verdict: **PENDING**
- exact accepted Plugin ZIP filename/size/SHA-256: **PENDING**

After owner `PASS`, no tracked runtime file may change without rebuilding and retesting the release
artifact.
