# v0.9.0 frozen-artifact manual acceptance

Status: **NOT YET RUN**. Complete this record only against the frozen
`MembraneVisualQC-0.9.0.zip` identified below. Prior real-PyMOL documentation and screenshot work
is useful exploratory evidence, but it is **not** this formal clean-install and v0.8.0-upgrade
acceptance run.

Do not mark a row PASS without observing it. Record deviations and attach exported evidence. Any
code or packaged-content change invalidates the artifact identity and every affected result.

## Tester, environment, and frozen artifact

- Tester:
- Date (UTC):
- OS and version:
- PyMOL edition and version:
- Python version reported by PyMOL:
- Qt binding and version (if available):
- Artifact filename: `MembraneVisualQC-0.9.0.zip`
- Artifact size (bytes): `205044`
- Artifact SHA-256: `9bab918b266db7f260820e6f6d13cc8ab579748a16c077ebd508e511a556c068`
- Preparation commit SHA: **PENDING COMMIT**
- Local artifact path:
- Evidence root directory:

Before starting, verify the file size and SHA-256 against the preparation report. Use no repository
checkout, `PYTHONPATH`, editable install, or files from the source tree.

## Exact linear procedure

Record `PASS`, `FAIL`, or `BLOCKED`, plus the evidence path, for every numbered step.

### A. Clean installation

1. Fully exit PyMOL. Remove any installed Membrane Visual QC plugin using Plugin Manager or remove
   its plugin directory. Confirm no `membrane_vqc` directory remains in that install location.
2. Start PyMOL with `PYTHONPATH` unset and without the repository as the working directory.
3. Install only the frozen ZIP through **Plugin > Plugin Manager > Install New Plugin**.
4. Fully exit and restart PyMOL. Open **Plugin > Membrane Visual QC**.
5. Record the displayed version; expected: `0.9.0`. Confirm exactly one menu entry and one dialog.
6. Record the plugin install directory and confirm later outputs/cache are not written there.

Result: ___  Evidence: ___

### B. Single structure

7. Load the checklist's retained single-structure fixture into one single-state object.
8. Select **Legacy global-z**, enter valid bounds, run QC, and record the outcome.
9. Enter `zmin >= zmax`; expected: inline/controlled validation failure, no run and no traceback.
10. Restore valid bounds and run **Show Slab**, **Colour Hydropathy**, and **Ligand Shell**.
11. Export JSON outside the plugin directory. Validate that it opens, declares software version
    `0.9.0`, and records the expected source/provenance.
12. Inspect the PyMOL console: expected no uncaught traceback from any normal input error above.

Result: ___  Exported evidence: ___

### C. Orientation sources

13. Run the retained planar-orientation-file fixture; confirm source provenance and validation.
14. Run the retained local PDBTM JSON/transformed-PDB pair; confirm both members and provenance.
15. Populate/use the PDBTM validated cache, then run from the validated cached pair; record the
    snapshot/provider evidence and confirm user cache is outside the plugin directory.
16. Exercise one missing pair member and one corrupt cache entry. Expected: controlled,
    path-safe failure with no traceback and no stale successful report.
17. Exercise the retained invalid planar orientation. Expected: controlled validation failure.

Result: ___  Exported evidence: ___

### D. Batch review

18. Validate the retained five-mode `mvqc-batch-plan-1.0` plan. Confirm five ordered jobs:
    `legacy_global_z`, `planar_orientation`, `pdbtm_local`, `pdbtm_cache`, and
    `pdbtm_opm_comparison`.
19. Execute all five jobs. Inspect both `SUCCESS` and `REVIEW_ITEMS` job outcomes and the typed
    overall outcome; these are operational/review states, not biological verdicts.
20. Select a result and open its manifest, output directory, report, and available CSV.
21. Start a second run and request cancellation (or perform the checklist's controlled
    interruption). Confirm a valid `CANCELLED`/partial manifest and no corrupt half-written file.
22. Reopen/recover the completed or interrupted result from its `batch-result.json`; inspect the
    current-session history and record the documented session-history limitation after restart.

Result: ___  Manifest/output/report evidence: ___

### E. PDBTM-OPM comparison

23. Run the retained comparison job using the validated PDBTM source and explicit local OPM file.
24. Confirm the result is comparable and records a measurable geometric difference (angle, center,
    or thickness), provenance for both sources, and a comparison band.
25. Export the comparison report outside the plugin directory.

Result: ___  Exported report: ___

### F. Paths and persistence

26. Repeat an export/run to an output path containing spaces.
27. Repeat to a Unicode output path.
28. Fully restart PyMOL, reopen the plugin, and reopen the retained result where supported.
29. Compare the plugin install directory to the inventory captured in A. Expected: no report,
    cache, session, temporary, or bytecode writes created by plugin workflows.

Result: ___  Evidence: ___

### G. Upgrade from v0.8.0

30. Fully exit PyMOL and remove v0.9.0. Install the frozen official v0.8.0 ZIP whose checksum is
    recorded in `docs/v0.8.0_release_evidence.json`; restart and verify v0.8.0 runs.
31. Create/retain one valid v0.8.0 report and batch plan, and note the external cache/output paths.
32. Follow `docs/upgrade_guide.md` exactly: exit PyMOL, remove the v0.8.0 plugin directory, install
    the frozen v0.9.0 ZIP, and fully restart PyMOL.
33. Verify displayed version `0.9.0`, exactly one dialog/menu entry, and no stale v0.8.0 GUI or
    command module. Run one single-structure job and one batch job.
34. Open/validate the retained old report and batch plan where the compatibility statement promises
    support. Confirm cache preservation or intentional invalidation exactly as documented.
35. Test rollback instructions: exit, remove v0.9.0, reinstall the verified v0.8.0 ZIP, restart,
    confirm v0.8.0, then return to the frozen v0.9.0 ZIP if final sign-off is intended for v0.9.0.

Result: ___  Upgrade/rollback evidence: ___

## Failures and deviations

For each failure record step, exact input, observed message, console output, evidence path, and
whether the artifact/code changed afterward.

- None recorded / details:

## Final sign-off

- Clean-install result:
- v0.8.0-to-v0.9.0 upgrade result:
- Restart/persistence result:
- Single-structure result:
- Orientation-source result:
- Batch-review result:
- Comparison result:
- Paths/no-install-writes result:
- Deviations accepted by owner:
- Final decision: `PASS` / `FAIL` / `BLOCKED`
- Owner/tester signature or GitHub comment URL:
- Sign-off date (UTC):

Publication remains blocked until this record is complete, every required row passes or has an
explicitly accepted non-blocking deviation, CI is green, and the artifact identity still matches.
