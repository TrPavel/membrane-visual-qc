# 1.0.0rc1 focused manual acceptance

Status: **NOT YET RUN**. Do not infer PASS from v0.9.0 acceptance or automated CI. Run this
procedure only against the exact frozen RC artifact recorded below.

## Tester, environment, and artifact

- Tester:
- Date/time (UTC):
- OS and version:
- PyMOL edition and version:
- Python version reported by PyMOL:
- Qt binding/version, if available:
- Packaged-content preparation commit: `62c754cce606043e26f1ec25c76ebcaa521b29aa`
- Artifact: `dist/MembraneVisualQC-1.0.0rc1.zip`
- Size: `205048` bytes
- SHA-256: `6ab5e5e6c2c96110f00510456d3041ccc286e5bc4643f8c463b01b090b3d4006`
- Members: `58`, ordinally sorted under `membrane_vqc/`
- Build environment: Windows 11 `10.0.26200`, CPython `3.12.13`, setuptools `83.0.0`, wheel
  `0.46.3`
- Evidence root:

Use no source checkout, `PYTHONPATH`, editable install, or repository working directory during the
installed-plugin checks. Record `PASS`, `FAIL`, or `BLOCKED` and an evidence path for every phase.

## A. Clean install and identity

1. Fully exit PyMOL and remove any existing Membrane Visual QC installation.
2. Start PyMOL without repository paths, install only the frozen RC ZIP through Plugin Manager,
   fully exit, and restart.
3. Open **Plugin > Membrane Visual QC**. Confirm exactly one menu entry/dialog and displayed version
   `1.0.0rc1`.

Result: ___  Evidence: ___

## B. Single structure and controlled invalid input

4. Load the retained single-state fixture and run one valid **Legacy global-z** QC workflow.
5. Exercise **Show Slab**, hydropathy colour, and ligand shell; confirm no traceback and no input
   coordinate mutation.
6. Enter `zmin >= zmax`; confirm a controlled validation message, no run, no traceback, and no stale
   successful result.

Result: ___  Evidence: ___

## C. Orientation sources

7. Run one retained planar-orientation file and confirm source provenance.
8. Run one retained local PDBTM JSON/transformed-PDB pair.
9. Use one validated cached PDBTM pair; confirm the cache remains outside the plugin directory and
   no implicit network request occurs.

Result: ___  Evidence: ___

## D. Five-mode batch and comparison

10. Validate and execute a five-job `mvqc-batch-plan-1.0` plan containing `legacy_global_z`,
    `planar_orientation`, `pdbtm_local`, `pdbtm_cache`, and `pdbtm_opm_comparison` in order.
11. Inspect the typed per-job and overall operational outcomes; do not interpret them as biological
    verdicts.
12. Open the comparison result and confirm provenance plus angle/center/thickness evidence and a
    comparison band, without a preferred-source or consensus claim.
13. Start a second batch, request cancellation, and confirm a valid cancelled/partial manifest;
    reopen the retained result and verify recovery/integrity handling.

Result: ___  Manifest/comparison evidence: ___

## E. Export and restart

14. Export JSON and available CSV outside the plugin directory, including a path with spaces or
    Unicode. Confirm the JSON declares `1.0.0rc1` and expected provenance.
15. Fully restart PyMOL, reopen the dialog/result where supported, and confirm no duplicate UI,
    stale module, uncaught traceback, or `QThread` warning.

Result: ___  Export/restart evidence: ___

## F. Upgrade from v0.9.0 and rollback

16. Fully exit PyMOL. Clean-install the published checksum-verified v0.9.0 ZIP and confirm version
    `0.9.0` plus one valid retained report/plan and external cache/output path.
17. Follow `docs/upgrade_guide.md`: fully exit, remove the v0.9.0 plugin directory, install the
    frozen RC ZIP, restart, and confirm version `1.0.0rc1`, one menu/dialog, one single-structure
    run, and one batch run.
18. Confirm supported v0.9.0 reports/plans and external cache/output state remain readable or are
    rejected only where the documented compatibility boundary says so.
19. Roll back: fully exit, remove RC, reinstall the verified v0.9.0 ZIP, restart, and confirm
    version `0.9.0`. Reinstall RC only if final sign-off is intended for the RC candidate.

Result: ___  Upgrade/rollback evidence: ___

## Final sign-off

- Clean install/version: ___
- Single structure/invalid input: ___
- Planar and PDBTM local/cache: ___
- Five-mode batch/cancellation/recovery: ___
- Comparison: ___
- Export/restart: ___
- v0.9.0 upgrade/rollback: ___
- Deviations accepted by owner: ___
- Final decision: `PASS` / `FAIL` / `BLOCKED`
- Owner/tester signature or GitHub comment URL: ___
- Sign-off date/time (UTC): ___

Do not tag or publish `1.0.0rc1` until this record is complete, required CI is green, and the RC ZIP
identity remains byte-identical.
