# 1.0.0rc1 focused manual acceptance

Status: **PASS — OWNER OBSERVED.** Do not infer a PASS from v0.9.0 acceptance or automated CI.
This procedure applies only to the exact frozen RC artifact recorded below.

## Tester, environment, and artifact

- Tester: repository owner (interactive visual sign-off supplied in the acceptance session)
- Date/time started (UTC): `2026-08-06T14:50:36Z`
- OS and version: Microsoft Windows 11 Home, 64-bit, version `10.0.26200`, build `26200`
- PyMOL edition and version: Incentive PyMOL `3.1.8` (`ba223be922270280eb2c8b5b5ec678811d6933dd`)
- Python version reported by PyMOL: `3.10.20`, conda-forge build, MSC v.1944 64-bit
- Qt binding/version: Qt `5.15.15`; PyQt `5.15.11`
- Packaged-content preparation commit: `62c754cce606043e26f1ec25c76ebcaa521b29aa`
- Artifact: `dist/MembraneVisualQC-1.0.0rc1.zip`
- Size: `205048` bytes
- SHA-256: `6ab5e5e6c2c96110f00510456d3041ccc286e5bc4643f8c463b01b090b3d4006`
- Members: `58`, ordinally sorted under `membrane_vqc/`
- Build environment: Windows 11 `10.0.26200`, CPython `3.12.13`, setuptools `83.0.0`, wheel
  `0.46.3`
- Evidence root: `C:\MVQC_acceptance\1.0.0rc1`

Use no source checkout, `PYTHONPATH`, editable install, or repository working directory during the
installed-plugin checks. Record `PASS`, `FAIL`, or `BLOCKED` and an evidence path for every phase.

## A. Clean install and identity

1. Fully exit PyMOL and remove any existing Membrane Visual QC installation.
2. Start PyMOL without repository paths, install only the frozen RC ZIP through Plugin Manager,
   fully exit, and restart.
3. Open **Plugin > Membrane Visual QC**. Confirm exactly one menu entry/dialog and displayed version
   `1.0.0rc1`.

Result: **PASS — OWNER OBSERVED**

Evidence and observations:

- Neutral artifact: `C:\MVQC_acceptance\1.0.0rc1\MembraneVisualQC-1.0.0rc1.zip`.
- Pre-flight verified `205048` bytes, the recorded SHA-256, byte identity with the repository
  artifact, and 58 unique ordinally sorted members under `membrane_vqc/`.
- `PYTHONPATH` was empty, the repository was absent from `sys.path`, all PyMOL processes were
  closed, and a neutral-directory probe found no importable `membrane_vqc` before installation.
- The prior installed plugin directory was removed exactly and the RC was installed only from the
  neutral ZIP through Plugin Manager, followed by a full restart.
- Installed `constants.py` reports `1.0.0rc1`. The owner visually confirmed the exact title
  `Membrane Visual QC 1.0.0rc1`, one menu entry/dialog, successful GUI opening, no traceback, and
  no missing assets.
- Owner phase verdict supplied interactively: `PASS`.

## B. Single structure and controlled invalid input

4. Load the retained single-state fixture and run one valid **Legacy global-z** QC workflow.
5. Exercise **Show Slab**, hydropathy colour, and ligand shell; confirm no traceback and no input
   coordinate mutation.
6. Enter `zmin >= zmax`; confirm a controlled validation message, no run, no traceback, and no stale
   successful result.

Result: **PASS — OWNER OBSERVED**

Evidence and observations:

- Neutral fixture used from `C:\MVQC_acceptance\1.0.0rc1\fixtures\local_context_review.pdb`;
  PyMOL did not load the repository checkout.
- The owner confirmed the valid Legacy global-z run, controlled `zmin >= zmax` validation with no
  traceback or stale successful result, and successful Show Slab, Colour Hydropathy, and Ligand
  Shell actions.
- Exports:
  `C:\MVQC_acceptance\1.0.0rc1\evidence\phase2\legacy-global-z.json` (6609 bytes, SHA-256
  `9bd2b13925cc173bccc3dd85c94ae7c9aba36de026cc1c1674d210497b29bd7d`) and its CSV companion
  (532 bytes, SHA-256 `f252fc4a11ebb3e0211f7c5912f42853f98214d35d4a641d150f1d8585b1f391`).
- Installed-RC validation passed with schema `1.1`, report type `single_structure_review`, software
  version `1.0.0rc1`, orientation source `manual_global_z`, and operational status `REVIEW_ITEMS`.
  The validator imported from the installed plugin directory with `PYTHONPATH` empty.
- Owner phase verdict supplied interactively: `PASS`; no traceback, warning, or unexpected behavior
  was reported.

## C. Orientation sources

7. Run one retained planar-orientation file and confirm source provenance.
8. Run one retained local PDBTM JSON/transformed-PDB pair.
9. Use one validated cached PDBTM pair; confirm the cache remains outside the plugin directory and
   no implicit network request occurs.

Result: **PASS — OWNER OBSERVED**

Evidence and observations:

- All inputs were neutral copies under `C:\MVQC_acceptance\1.0.0rc1\fixtures`; no PyMOL workflow
  loaded a repository path.
- Planar export `evidence\phase3\planar.json`: 3436 bytes, SHA-256
  `c7724394459f77e4d269e758f4b85597a55b661c55bc1725a457bb6d28994941`; installed-RC validation
  passed as schema `1.1`, source `synthetic_rigid_transform`, status `REVIEW_ITEMS`.
- PDBTM-local export `evidence\phase3\pdbtm-local.json`: 10281 bytes, SHA-256
  `d459179d8329f12fdb7d20111d6baa23697b21b77691a9e0a591f4c274594374`; installed-RC validation
  passed as schema `1.3`, source `pdbtm_offline`, status `NO_FLAGS`.
- The retained synthetic `9zzz` cache snapshot was reactivated through the installed RC cache API,
  not by editing cache files or fetching. Integrity and semantic revalidation passed; active
  snapshot `6c8e835b18198d9a657148e0f168181cbcda2205ecff62337352508b1190152d`, pair ID
  `6c47685a213b6dfca8a0a25551539d62a2932f5265485ff29e62b4b147dfdf4a`.
- The owner confirmed planar provenance/depth evidence, PDBTM-local mapping/applicability evidence,
  cached acquisition/applicability provenance with no fetch, and a clean missing-source failure
  without traceback or stale state.
- No separate cache-mode export was retained in this phase; export was not a phase requirement and
  the cache record itself was objectively integrity-checked. Owner phase verdict: `ALL PASS`.

## D. Five-mode batch and comparison

10. Validate and execute a five-job `mvqc-batch-plan-1.0` plan containing `legacy_global_z`,
    `planar_orientation`, `pdbtm_local`, `pdbtm_cache`, and `pdbtm_opm_comparison` in order.
11. Inspect the typed per-job and overall operational outcomes; do not interpret them as biological
    verdicts.
12. Open the comparison result and confirm provenance plus angle/center/thickness evidence and a
    comparison band, without a preferred-source or consensus claim.
13. Start a second batch, request cancellation, and confirm a valid cancelled/partial manifest;
    reopen the retained result and verify recovery/integrity handling.

Result: **PASS — OWNER OBSERVED**

The retained validated plan is
`C:\MVQC_acceptance\1.0.0rc1\phase4-plan\stage5a_batch_plan.json`. It contains, in order, the
five required jobs: `legacy`, `planar`, `pdbtm-local`, `pdbtm-cache`, and `comparison`.

The owner confirmed the full batch completed; recorded actual counts for `SUCCESS`,
`REVIEW_ITEMS`, and any other status; inspected the comparison angle, centre displacement,
thickness difference, both-source provenance, and review band; and verified Open result manifest,
Open output directory, Reveal report, and Reveal CSV. A second batch was cancelled, with completed
and cancelled job counts and its overall status recorded; both runs were reopened from
current-session history; and no uncaught traceback occurred.

The completed-run and cancellation-run output directories, including both `batch-result.json`
files, were later deleted to reclaim disk space. Their paths and detailed measurements are
therefore unavailable for retrospective recording. This is an evidence-retention limitation only;
the owner supplied the Phase 4 acceptance verdict before deletion.

## E. Export and restart

14. Export JSON and available CSV outside the plugin directory, including a path with spaces or
    Unicode. Confirm the JSON declares `1.0.0rc1` and expected provenance.
15. Fully restart PyMOL, reopen the dialog/result where supported, and confirm no duplicate UI,
    stale module, uncaught traceback, or `QThread` warning.

Result: **PASS — OWNER OBSERVED**

The owner confirmed JSON/CSV exports to normal, space-containing, and Unicode paths; no workflow
writes inside the installed plugin directory or `C:\Pymol_script_1`; a full PyMOL restart with
the RC still usable; and one invalid or read-only destination producing a controlled failure with
no traceback or partial output. Supporting output paths were intentionally not retained.

## F. Upgrade from v0.9.0 and rollback

16. Fully exit PyMOL. Clean-install the published checksum-verified v0.9.0 ZIP and confirm version
    `0.9.0` plus one valid retained report/plan and external cache/output path.
17. Follow `docs/upgrade_guide.md`: fully exit, remove the v0.9.0 plugin directory, install the
    frozen RC ZIP, restart, and confirm version `1.0.0rc1`, one menu/dialog, one single-structure
    run, and one batch run.
18. Confirm supported v0.9.0 reports/plans and external cache/output state remain readable or are
    rejected only where the documented compatibility boundary says so.
19. Roll back: fully exit, remove RC, reinstall the verified v0.9.0 ZIP, restart, and confirm
    version `0.9.0`. Reinstall RC only if final sign-off is intended for the RC candidate.

Result: **PASS — OWNER OBSERVED**

The owner completed the clean-replacement sequence: RC removal; verified official v0.9.0
installation and restart; v0.9.0 version and smoke workflow; clean RC installation and restart;
RC version/menu/stale-state checks; single-structure and five-mode batch workflows; retention of
plans, reports, external cache, and output data; rollback to v0.9.0; and final clean
reinstallation/restart of `1.0.0rc1`. No traceback was reported.

## Final sign-off

- Clean install/version: **PASS**
- Single structure/invalid input: **PASS**
- Planar and PDBTM local/cache: **PASS**
- Five-mode batch/cancellation/recovery: **PASS**
- Comparison: **PASS**
- Export/restart: **PASS**
- v0.9.0 upgrade/rollback: **PASS**
- Deviations accepted by owner: Phase 4 output evidence was deleted after owner verification because
  of disk-space pressure; paths, counts, and measurements are not available retrospectively.
- Final decision: **PASS — READY FOR FINAL SIGN-OFF**, subject to required CI and frozen-artifact
  identity remaining valid
- Owner/tester signature or GitHub comment URL: repository owner, interactive acceptance session;
  external signature/comment URL not recorded
- Sign-off date/time (UTC): 2026-08-07 (exact UTC time not recorded)

Do not tag or publish `1.0.0rc1` until this record is complete, required CI is green, and the RC ZIP
identity remains byte-identical.
