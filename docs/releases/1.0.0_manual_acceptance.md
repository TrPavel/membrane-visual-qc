# 1.0.0 final manual acceptance

Status: **UNCOMPLETED**.

**AUTOMATED FINAL GATE: pending until actually completed**
**OWNER FINAL ACCEPTANCE: NOT YET PERFORMED**

This checklist is for the final `1.0.0` candidate only. Do not copy outcomes from the owner-accepted
`1.0.0rc1` record. Record observations, environment, date, and PASS/FAIL only after an owner runs
this exact candidate.

## Candidate identity

- ZIP: `dist/MembraneVisualQC-1.0.0.zip`
- SHA-256 sidecar: `dist/MembraneVisualQC-1.0.0.zip.sha256`
- Acceptance copy: `C:\MVQC_acceptance\1.0.0\MembraneVisualQC-1.0.0.zip`
- Owner result: **NOT YET PERFORMED**

## Procedure

1. Copy the frozen ZIP to the acceptance path and verify its SHA-256 against the sidecar.
2. Use a clean PyMOL plugin installation and install the ZIP through Plugin Manager.
3. Restart PyMOL and confirm exactly one menu entry and version exactly `1.0.0`.
4. Run a valid Legacy global-z analysis.
5. Verify invalid `zmin`/`zmax` is rejected without a traceback.
6. Use Show Slab and confirm only plugin-owned slab objects are created.
7. Run Colour Hydropathy and confirm expected residue colouring.
8. Run Ligand Shell and confirm expected neighbour selections at the cutoff boundary.
9. Export JSON and confirm it is strict JSON (no `NaN` or `Infinity`).
10. Run a valid planar-orientation file workflow.
11. Run a valid offline PDBTM local JSON/transformed-PDB pair.
12. Run a validated PDBTM cache workflow without an unexpected network operation.
13. Try an invalid or corrupt orientation/source input and confirm a controlled message.
14. Run the five-mode batch plan.
15. Inspect a PDBTM/OPM comparison job.
16. Open and inspect the result manifest.
17. Open and inspect a produced report.
18. Open and inspect a produced CSV.
19. Exercise a result-browser diagnostic case (for example a missing selected report) and confirm a useful controlled message.
20. Cancel an in-progress batch and confirm the documented cancelled/recovery state.
21. Close and reopen the dialog; confirm expected session-history behavior and no duplicate UI.
22. Repeat a representative output flow at a normal writable path.
23. Repeat it at a path containing spaces.
24. Repeat it at a Unicode path.
25. Try an invalid or read-only output path and confirm controlled recovery.
26. Fully restart PyMOL and confirm no stale plugin UI or transient state persists.
27. Perform clean-replacement upgrade from `1.0.0rc1` to `1.0.0`.
28. Confirm retained plan, report, and `cache-v1` compatibility.
29. Roll back by clean replacement from `1.0.0` to `1.0.0rc1`.
30. Reinstall `1.0.0` as the final installed version and restart again.
31. Record owner visual and scientific-review sign-off, or any exceptions, below.

## Owner record

- Environment: _
- Date: _
- Observations: _
- Final owner decision: **NOT YET PERFORMED**
